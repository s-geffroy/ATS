[tool.ruff]
line-length = 100
target-version = "py39"

[tool.ruff.lint]
select = ["E", "F", "I", "B", "UP", "SIM"]
ignore = [
  "E501",  # line length handled by formatter
  "UP007", # keep Union[X, None] readable in places
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["B011"]
